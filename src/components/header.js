import React from 'react';
import headerPlaceholder from '../images/header-placeholder.png';

const Header = () => {
  return (
    <img src={headerPlaceholder} style={{width: 100 + '%'}} />
  )
}

export default Header;
